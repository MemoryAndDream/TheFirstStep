程序逻辑：
配置文件目录，程序会遍历里面的所有doc,docx,pdf文件提取信息
追加写入csv

遇到解析失败的输出文件名

目前没有测试文件过多是否会有内存问题之类

由于投资者关系活动主要内容介绍字段会导致每一条记录比较大，如果跑多了估计打不开


可以根据代码和原excel合并


由于使用的第三方解析docx模块不支持python3.6，所以需要用虚拟环境运行或打包exe运行

保存文件的时候不能打开excel